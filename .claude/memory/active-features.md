# memory/active-features.md

> Features currently in progress. Move to `completed-features.md` when done (and run the
> Documentation Rules). Keep this list honest and small.

**Last updated:** 2026-07-20

| Feature | Milestone | Owner | Status | Notes |
|---------|-----------|-------|--------|-------|
| STOS founder-experience redesign | ADR-008 | — | ✅ live on :3000 | 6-item IA (Home/Assistant/Work/People/Knowledge/Workspace), deep-navy glass design system, AI-first. Backend untouched. Demo data seeded (`npm run demo-seed`). |
| **Consolidation Program — Sprint 1: Foundation Audit & Navigation** | ADR-009 | — | ✅ done + verified live | Deleted orphans `/dashboard` + `/leads` and dup components (leads-table, ai-chat); all denied-guards → `/home`; added `(app)/loading.tsx`, `(app)/error.tsx`, global `not-found.tsx`; reskinned all remaining OLD pages (People/Knowledge/Connectors/Sign-in/admin/*) to `.glass`/`.btn-*`/tokens; `/workspace` empty state; `EmployeesTable` header now a prop. New route map: `knowledge/architecture/routing.md`. typecheck+lint+build exit 0. |
| **Sprint 2: Organization Engine** | ADR-010 | — | ✅ done + verified live | New `org-console.tsx` (collapse, inline rename, drag-drop reparent + move-to-root, manager dropdown, capacity/vacancy chips, add/delete). Backend: `headcountCapacity`, `listWithStats`, descendant-cycle move guard, nullable parent/manager. 4 AI tools. Verified live incl. AI "Rahul reports to Anita". |
| **Sprint 3: Lead Intelligence & CRM** | ADR-011 | — | ✅ done + verified live | Notion-style lead page `/work/[id]` (AI summary + contacts/tasks/meetings/proposals/timeline + editable intelligence sidebar). AI lead summary route + `ai/summarize-lead.ts`. Grid rows deep-link. |
| **Sprint 4: Outbound Sales OS** | ADR-012 | — | ✅ done + verified live | Lead source/campaign/touchCount/lastTouchAt; touch endpoint + engagement UI; AI log_touch. Email/sequences deferred to Sprint 8. |
| **Sprint 5: AI Assistant v2 (OS brain)** | ADR-013 | — | ✅ done + verified live | Live context injection; STOS identity; tools list_leads/find_stale_leads/assign_leads. Verified live. |
| **Sprint 6: Task & Workflow Engine** | ADR-014 | — | ✅ done + verified live | Task followers/comments/recurrence/deps; assignment notifications; bulk assignToRole/Unit; comment/follow endpoints; recurring spawn; AI assign_task/assign_task_to_role. Verified live. |
| **Sprint 7: Knowledge Graph & Org Memory** | ADR-015 | — | ✅ done + verified live | `dossierService.forName` + AI `entity_dossier`. Verified live grounded w/ dates+actors. |
| **Sprint 8: Integrations Layer** | ADR-016 | — | ✅ done + verified live | `connectorService.test` + `POST /api/connectors/[kind]/test`; `markStatus`; connectors UI (status dots/last-synced/Test/Reconnect/Disconnect, grouped). Extensible registry. Verified live. |
| **Sprint 9: Executive OS (dashboards)** | ADR-017 | — | ✅ done + verified live | `metricsService.summary`; persona-aware `Dashboard` on Home (leaderboard+₹ gated). Verified owner vs sales_rep. |
| **Final Validation Sprint** | ADR-018 | — | ✅ PASS | 5-persona nav matrix (no leaks); E2E lead→touch→meeting→WON; owner approved quote; AI dossier/assign/touch/org-edit; RAG grounded+cited. **STOS Consolidation Program (Sprints 1–9) COMPLETE.** |
| **Sprint 12D: Production Experience** | ADR-022, ADR-023 | — | ✅ done + verified live | **Demo/Production isolation**: `workspace` tag on content collections (leads/tasks/meetings/proposals/activities/docs/…) + Qdrant payload; reads/writes filtered by active mode (`lib/workspace.ts` ← org mode); identity/config shared; `WorkspaceToggle` on Home + sidebar "Demo" pill. Verified: demo 18 leads → production 0 → restore. **Realism**: lead ICP+intent+AI summary, company revenue, employee/dept/playbook KPIs, Founder Sales team, client/meeting notes (`npm run simulate`). **Executive dashboard**: owner Home = KPI tiles + Chief-of-Staff AI briefing (`/api/command-center/briefing`) + attention panels. **Branding/polish**: `<Logo/>`, branded sign-in, `.animate-in`, focus rings, EmptyState icons. **⌘K palette**: `CommandPalette` + global search (`/api/search`). typecheck+lint+build 0. |
| **Sprint 12: SVT Org & Real Operations** | ADR-021 | — | ✅ done + verified live | **Simulation** `npm run simulate` (`scripts/simulate.ts`): 8 departments, 8 playbooks, conveyor teams, 16 companies/8 industries, 18 leads all stages BACKDATED 60–90d (time-machine sweep), per-person tasks, past+future meetings, 6 knowledge docs, proposals+approvals — makes STOS look 3 months live. **Demo/Production mode** (`src/lib/mode.ts` + `/admin/settings` + `GET/PATCH /api/admin/mode`; simulate guarded). **People Admin** `/admin/employees` create+invite (`employeeService.create` provisions account, `POST /api/admin/employees`) + inline dept/manager/role/status/model; `Employee.defaultExecutionModel`. **Owner Command Center** `/command` (sidebar for owner): `commandCenterService` (yesterday/blocked/needs-help/at-risk/forecast/utilization/bottlenecks) + AI recs (`POST /api/command-center/recommendations`). typecheck+lint+build 0; live on :4000. |
| **Sprint 11: Deferred Work-Engine surfaces** | ADR-020 | — | ✅ done + verified live | Work views (List/Board/Calendar/Workload) on `/work/tasks`; **Conveyor throughput dashboard** `/work/conveyor` (`conveyor-metrics-service` + `GET /api/metrics/conveyor`, new WorkTabs "Conveyor" gated `users.read`); **Sales-model admin** `/admin/sales-models` (create/delete playbooks + conveyor teams; new `DELETE /api/playbooks/[id]`; Workspace card); **Calendar auto-schedule** `POST /api/work/schedule` → Google Calendar, degrades gracefully (`connected:false`, no 500). Added `.inp` class. Google Calendar connect issue diagnosed = **config, not code** (redirect-URI whitelist + OAuth test-user; see known-issues). typecheck+lint+build 0; smoke on :3000 all 200. |
| **Sprint 10: Work Execution Engine** | ADR-019 | — | ✅ done + verified live | 10A: employee fields (title/skills/capacity/availability) + **Deblina** (marketing) + reporting; `/work` tabbed (Leads·Tasks); `/work/tasks` My/Team/All (RBAC server-side). 10B: Lead executionModel individual/conveyor + Playbooks + Conveyor Teams + `assertCanModify` autonomy + SLA/handoff on advance + model panel on lead detail. 10C: AI daily briefing (`/api/work/briefing`) + `find_sla_breaches`. Scripts: `add-people`, `seed-sales`. Verified live all phases. Deferred: calendar/board/workload views, conveyor dashboard, playbook/team admin UI. |
| Full v1 backend | M1–M8 | — | ✅ builds + Mongo live | Leads/Companies/Contacts/Tasks/Meetings CRUD + stage engine + Proposals/Quotations + AI chat + dashboards + activities/notifications/audit/settings. |
| Knowledge engine + connectors (ABOS capstone) | M10 | — | ✅ builds + core verified live | Qdrant semantic memory + ingestion + RAG Q&A (RBAC + citations, no-hallucination) + docs + Google Calendar OAuth + connector architecture + AES-256-GCM encryption + observability. Qdrant/Voyage/Google need real services to go fully live. |
| Metadata-driven platform (configurable BOS) | M12 | — | ✅ builds + verified live w/ real Qdrant | Dynamic org tree (orgUnits), custom object platform (objectDefinitions + generic customRecords, auto CRUD/RBAC/audit/AI-search), workflow/approval engine, policy engine, AI Organization Admin tools, org timeline. **61 API routes.** lint+typecheck+build exit 0. Verified live: real Qdrant health, knowledge upload→embed→cited grounded answer, India→Hyderabad org tree. Voyage key needed for full semantic quality. |
| Enterprise IAM (identity & access foundation) | M11 | — | ✅ builds + RBAC verified live | Centralized permission engine (dotted catalog, one `can`/`assertPermission`), 14 system roles + custom roles + per-user overrides, **Owner = `*` bypass**, role sourced from `employees` directory (not Better Auth), legacy colon-perms mapped into the same engine, env-driven Owner bootstrap, org→dept→team→employee, admin UI (employees/roles/audit), finance isolation, append-only audit. **48 API routes.** lint+typecheck+build exit 0. Verified live: Owner `*`, sales_rep denied finance+admin (403), owner bootstrap. |

## Notes
Backend for the full v1 surface is written, typechecks, and builds cleanly (31 routes, 2
pages). API contract documented in `../knowledge/api/endpoints.md`. Remaining before
`completed-features.md`: run against real `MONGODB_URI` + `ANTHROPIC_API_KEY` and drive the
flows. UI is intentionally thin (Dashboard + Leads + AI chat); other objects are API-first.
See `technical-debt.md` (pagination, streaming, adaptive-thinking SDK note).
